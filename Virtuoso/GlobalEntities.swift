import RealityKit

/// The root entity for entities placed during the game.
let spaceOrigin = Entity()

/// The anchor entities used to configure the keyboard
var leftAnchor = Entity()
var rightAnchor = Entity()
